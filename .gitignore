.DS_Store
Thumbs.db
node_modules/
.env
*.log
__pycache__/
*.pyc

# artefacts de sondage des revues (ne jamais publier : le dépôt se déploie tel quel)
api/_probe*.mjs
node_modules

# Artefacts de transcription à la RACINE (doublons de travail — la sauvegarde
# organisée et versionnée vit dans chiourim/). Empêche tout git add accidentel.
/chiour*
/build_subs*.py
/apply_fixes*.py
/description*.txt
/post-whatsapp-*.txt
/resume-chiour*.md
/cours_*.srt
/*.vtt

# Répertoires de travail des agents (règle 15 : un sous-dossier par agent)
scratchpad/



# Sortie de build : 34 Mio réécrits à chaque déploiement, et GitHub REFUSE
# un fichier de plus de 100 Mio. Régénéré par vercel-build avant le bundling
# des fonctions ; déclaré dans vercel.json (includeFiles) pour ne pas dépendre
# du traçage automatique. Après un clone frais : `npm run build`.
data/corpus-shabbat.json
data/corpus-index.br

# rapports de citations ciblés (--path) : le rapport de référence est audit/citations-verifiees.csv
audit/citations-sources-*.csv

# cache local du garde-fou d'alignement (rejouable, non versionne)
scripts/.cache-sefaria-alignement/
